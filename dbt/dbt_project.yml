name: 'granger_knows_why'
version: '1.0.0'
config-version: 2

profile: 'granger_profile'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  granger_knows_why:
    bronze:
      +materialized: view
    silver:
      +materialized: view
    gold:
      +materialized: table
